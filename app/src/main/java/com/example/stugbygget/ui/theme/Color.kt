package com.example.stugbygget.ui.theme

import androidx.compose.ui.graphics.Color

// ═══════════════════════════════════════════════════
// StugBygget — Colour Palette "Svensk Sommar"
// ═══════════════════════════════════════════════════

// ── Primary: Falu Red ──
val FaluRed = Color(0xFF8B2E16)
val FaluRedLight = Color(0xFFA8432A)
val FaluRedDark = Color(0xFF6B1E0E)
val FaluRedContainer = Color(0xFFFDE8E0)
val OnFaluRedContainer = Color(0xFF3B0E04)

// ── Secondary: Lake Blue ──
val LakeBlue = Color(0xFF2E6B8A)
val LakeBlueLight = Color(0xFF3A87AD)
val LakeBlueDark = Color(0xFF1E4B62)
val LakeBlueContainer = Color(0xFFD6EBF5)
val OnLakeBlueContainer = Color(0xFF0A2A3A)

// ── Tertiary: Meadow Green ──
val MeadowGreen = Color(0xFF4A7C59)
val MeadowGreenLight = Color(0xFF5D9B6E)
val MeadowGreenDark = Color(0xFF2D5A3A)
val MeadowGreenContainer = Color(0xFFDBF0E0)
val OnMeadowGreenContainer = Color(0xFF0E2A16)

// ── Accent: Midsummer Gold ──
val MidsummerGold = Color(0xFFD4A843)
val MidsummerGoldLight = Color(0xFFE8C65A)
val MidsummerGoldDark = Color(0xFFAA842E)
val MidsummerGoldContainer = Color(0xFFFFF3D6)
val OnMidsummerGoldContainer = Color(0xFF3A2A08)

// ── Neutral: Wood Warm ──
val WoodWarm = Color(0xFF6B4C30)
val WoodWarmLight = Color(0xFF8B6914)
val WoodWarmDark = Color(0xFF4A3420)
val Sand = Color(0xFFC4A97D)
val SandLight = Color(0xFFDBC8A0)

// ── Background & Surfaces ──
val CreamBackground = Color(0xFFFEFCF6)
val ParchmentSoft = Color(0xFFFBF7EE)
val CardWhite = Color(0xFFFFFFFF)

// ── Text ──
val TextDark = Color(0xFF2C1810)
val TextMedium = Color(0xFF5C4A3A)
val TextLight = Color(0xFF8B7B6B)
val TextOnPrimary = Color(0xFFFFFFFF)
val TextOnSurface = Color(0xFF2C1810)

// ── Borders & Dividers ──
val Border = Color(0xFFE8DFD0)
val BorderLight = Color(0xFFF0EAE0)
val Divider = Color(0xFFE8DFD0)

// ── Semantic colours ──
val Success = MeadowGreen
val SuccessContainer = MeadowGreenContainer
val Warning = MidsummerGold
val WarningContainer = MidsummerGoldContainer
val Error = Color(0xFFB3261E)
val ErrorContainer = Color(0xFFF9DEDC)
val Info = LakeBlue
val InfoContainer = LakeBlueContainer

// ── Phase colours (timeline) ──
val PhaseRivning = FaluRed
val PhaseTak = LakeBlue
val PhaseFonster = MeadowGreen
val PhaseVVS = LakeBlueLight
val PhaseEl = MidsummerGold
val PhaseMalning = FaluRedLight
val PhaseGolv = WoodWarm
val PhaseAltan = MeadowGreenLight

// ── Gallery category colours ──
val GalleryBefore = FaluRed
val GalleryDuring = MidsummerGold
val GalleryAfter = MeadowGreen
